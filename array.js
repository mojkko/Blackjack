let fruits = ["orange", "apple", "apple", "orange", "orange"];
let apples = [];
let oranges = [];

function sortfruit() {
  for (let i = 0; i < fruits.length; i++) {
    if (fruit[i] === "orange") {
      apples.push(i);
    } else {
      oranges.push(i);
    }
  }
}

console.log(oranges);
